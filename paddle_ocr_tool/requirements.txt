# PaddleOCR 截图文字识别工具 - 依赖列表
# 安装命令：pip install -r requirements.txt

# 核心依赖
paddleocr>=3.0.0
paddlepaddle>=3.0.0
Pillow>=10.0.0

# Windows 平台增强功能（可选但推荐）
keyboard>=0.13.5; sys_platform == 'win32'
pystray>=0.19.5; sys_platform == 'win32'
pywin32>=300; sys_platform == 'win32'

# 打包工具（仅开发环境需要）
# pyinstaller>=5.0.0
